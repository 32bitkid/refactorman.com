<article>
	<header>
		<time datetime='<%= date_xml(page.date) %>'><%= date(page.date) %></time>
		<a href="<%= url_for() %>" class="title"><%= config.title %></a>
		<a href="mailto:32bitkid@gmail.com" rel='author'><%= config.author %></a>
	</header>
	<h1><%= page.title %></h1>
	<%- page.content %>
</article>
